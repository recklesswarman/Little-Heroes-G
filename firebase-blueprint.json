{
  "entities": {
    "users": {
      "title": "User Profiles & Hero Stats",
      "description": "Collection of kid hero profiles saving stats, coins, points, XP, inventory, equipped gear, and pet status",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "role": { "type": "string" },
        "avatar": { "type": "string" },
        "heroLevel": { "type": "number" },
        "heroXp": { "type": "number" },
        "maxHeroXp": { "type": "number" },
        "coins": { "type": "number" },
        "points": { "type": "number" },
        "streakDays": { "type": "number" },
        "energy": { "type": "number" },
        "equippedStickers": { "type": "array", "items": { "type": "string" } },
        "cardSkin": { "type": "object" },
        "inventory": { "type": "array", "items": { "type": "string" } },
        "pet": { "type": "object" },
        "updatedAt": { "type": "string" }
      },
      "required": ["id", "name", "coins", "points", "heroXp"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /users/{userId} {\n      allow read, write: if true;\n    }\n    match /{document=**} {\n      allow read, write: if true;\n    }\n  }\n}"
}
